import { styled } from "../../theme";

export const Background = styled("div", {
    background: "$gradient",
    width: "100vw",
    height: "100vh",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",

    variants: {
        
    }
})