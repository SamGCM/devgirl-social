import { styled } from "../../theme";

export const Input = styled("input", {
    background: "$white026",
    borderRadius: "$5",
    outline: "none",
    border: "none",
    padding: "$2 $3",
    fontSize: "$3",
    color: "$white",
    fontWeight: "$semiBold"
}) 