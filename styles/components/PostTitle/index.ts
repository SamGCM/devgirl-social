import { styled } from "../../theme";

export const PostTitle = styled("h3",{
    color: "$white",
    cursor: "pointer",
    variants: {
        marginLeft: {
            7: {
                marginLeft: "$7"
            }
        },
        
    }
})